# Commissioning (v0.1)

Commissioning evidence should include:
- sensor functional checks (pass/fail + timestamp)
- interlock verification (trip -> shutdown -> reset)
- emergency stop validation
- documentation signoff

Record evidence references as `permit_artifact` entries for YieldOS.
