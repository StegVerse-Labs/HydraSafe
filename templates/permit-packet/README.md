# Permit Packet Template

This directory contains a documentation-only HydraSafe permit-packet framework. It does not issue permits, certify code compliance, provide engineering approval, or authorize physical operation.

Use `permit-packet.template.json` as an envelope baseline. Replace demo scope values, preserve the denied authority boundary, identify jurisdictional/OEM sources, and obtain external review from the authority having jurisdiction and qualified professionals where required.
